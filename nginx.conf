server {
  listen 3080;

  server_name _;

  root /usr/share/nginx/html;
  index index.html;

  location / {
    try_files $uri /index.html;
  }

  # Optional: proxy API requests to backend
  location /api/ {
    proxy_pass http://api:8000/;  # match your docker-compose backend service name
  }
}